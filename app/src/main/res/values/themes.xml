<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GigongRequest" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="android:spinnerStyle">@style/CustomSpinnerStyle</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="SpinnerStyle">
        <item name="android:background">@drawable/spinner_bg</item>
    </style>
    <style name="CustomDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@drawable/option_block</item>
        <item name="android:windowMinWidthMajor">90%</item> <!-- 가로 너비를 조절하는 비율 -->
        <item name="android:windowMinWidthMinor">90%</item>

    </style>
    <style name="CustomSpinnerStyle" parent="@android:style/Widget.Spinner">
        <item name="android:background">@drawable/custom_spinner_backgroundlayout</item>
    </style>
</resources>